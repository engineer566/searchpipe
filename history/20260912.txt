1. 优化服务条款声明，跟订阅制和积分制匹配（订阅制积分非永久有效）
2. 优化未勾选服务条款的文案，体现“同意服务条款”
3. 失败自动返还积分，需要验证，如果未实现则需要实现
4. 默认API_key：邮箱验证完成后即自动生成一个默认API Key，用户默认配置MCP也默认使用这个API Key。API Key可以查看（需要手动点击显示，平时在隐藏状态，而不是一次性的）。MCP链接生成也可以选择其它API Key，可以增加一个复选框来选择
5. 优化一句话配置流程，一句话里信息里默认带有API Key信息，这样Agent可以一键配置。但是这个一句话不显示，只保留复制按键
6. 取消一经售出，拒不退款这类提示，这类提示在法律上无效
7. 充值功能也需要邮箱验证后才能做，避免发生财务纠纷