# 2026-09-11 邮箱验证功能开发

## 需求
防止虚假注册骗积分，添加邮箱验证功能。

## 实现内容

### 1. User 模型变更
- 新增 `email_verified` 布尔字段（默认 false）
- 迁移脚本：`alembic/versions/d4b8e2f1a3c5_add_email_verified_to_users.py`

### 2. 邮箱验证服务
- 新文件：`src/ai_search/auth/email_verification.py`
- Redis token 存储：`verify:token:<token>` → user_id（TTL 24h）
- 发信冷却：`verify:cooldown:<email>`（TTL 60s）
- 函数：`create_verification_token`、`consume_verification_token`、`send_verification_email`、`resend_verification_email`

### 3. API 端点
- `GET /auth/verify-email?token=xxx`：验证邮箱，成功跳转登录页带 `verify_success=1`
- `POST /auth/resend-verification`：重发验证邮件（需登录）
- `/auth/register`：注册后自动发送验证邮件
- `/auth/me`：返回 `email_verified` 字段

### 4. Dashboard 变更
- 注册成功后跳转到 `/dashboard/login?registered=1`，显示"请查收验证邮件"提示
- 登录页支持多种提示参数：`registered`、`verify_success`、`verify_error`、`verify_already`
- 未验证用户访问 dashboard 显示警告卡片 + 重发按钮
- 新增 `POST /dashboard/resend-verification` 端点

### 5. 模板更新
- `login.html`：已有 info/error 变量支持，无需修改
- `dashboard.html`：添加邮箱未验证警告卡片 + 重发成功/冷却提示

### 6. 测试用例
- `tests/test_auth.py` 新增 9 个测试：
  - `test_register_sends_verification_email`
  - `test_verify_email_success`
  - `test_verify_email_invalid_token`
  - `test_verify_email_already_verified`
  - `test_dashboard_register_shows_verification_hint`
  - `test_dashboard_unverified_user_sees_warning`
  - `test_resend_verification_email`
  - `test_api_resend_verification`
  - `test_api_resend_verification_already_verified`

### 7. 文档更新
- `docs/INDEX.md`：更新模块速查表、路由速查、按任务跳转表
- `docs/memory/searchpipe-auth-design.md`：更新设计决策和实现要点

## 待办
- [ ] 在测试环境运行全量 pytest
- [ ] 部署到测试服验证
- [ ] 提交到 dev 并 push origin

## 虎皮椒支付接入（微信单渠道，真实商户凭证）

需求：用户提供虎皮椒商户（仅开通微信支付），appid 201906187427，网关 https://api.xunhupay.com/payment/do.html。

改动：
- payments/provider.py 新增 available_channels()（默认双渠道）；xunhupay.py 按已配置凭证覆盖返回。
- payments/service.py 下单 pay_channel 可留空 → 自动落到首个已配置渠道；未开通渠道下单 400 并提示可用渠道。
- payments/routes.py /payments/catalog 的 pay_channels 改为动态（按 provider 已配置凭证）。
- 关键 bug 修复：虎皮椒签名不做 URL 编码（key 排序后原始值 key1=val1&... 直接拼 appsecret 再 MD5）。原实现 urlencode 导致中文 title 被百分号编码，网关报「错误的签名」。已用 1 分钱真实下单冒烟通过（网关返回微信收银台 URL）。
- billing.html 渠道单选/页头文案按 catalog 动态渲染；terms.html/pricing.html 支付文案改为「微信支付（以结算页实际可用渠道为准）」。
- .env 写入 XUNHUPAY_APPID_WECHAT/APPSECRET_WECHAT/NOTIFY_URL（本机 notify_url 为 localhost，仅联调占位）。
- tests/test_payments.py 新增单渠道商户（仅微信）4 个测试；全量 pytest 181 passed。

待办：
- ~~部署测试服/生产服配置凭证 + 回调地址~~ 已完成：2026-09-11 直接部署生产服（main 368f279），
  生产 .env 补 XUNHUPAY_APPID_WECHAT/APPSECRET_WECHAT，Playwright 6/6 验证通过（含 2 个真 bug 修复：
  billing 页 DOMContentLoaded、下单响应补 pay_channel）。详见 docs/memory/searchpipe-prod-server.md。
  测试服（47.98.124.167）尚未部署本次支付改动，需要时同样补 .env 三处即可。
- 真实微信付款回调链（虎皮椒→/payments/callback→发积分）待首笔真实收款观察日志确认。
- 后续若在虎皮椒后台开通支付宝应用，只需补 XUNHUPAY_APPID_ALIPAY/APPSECRET_ALIPAY，无需改代码。
